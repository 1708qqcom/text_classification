# 投满分项目 — 模拟面试提问（严厉面试官版）

> 面试官风格：追问到底、质疑一切、压力面。不给你"嗯嗯对对"的舒适区。

---

## 第1题 · 架构设计

你六个方案目录（02-rf 到 07-bert_prune）各有一套几乎一模一样的 `config.py`，连注释都复制粘贴。README 里也写了"统一公共配置模块"是待改进项——你都意识到问题了，为什么没改？

**追问**：如果我现在让你重构，你打算怎么设计？继承？组合？说清楚理由，别背设计模式名字。

---

## 第2题 · 知识蒸馏（压力题）

你说 06-bert_distill 做了知识蒸馏，07-bert_prune 做了权重剪枝。但我看了代码——`bert_train.py` 和 `bert_predict_fun.py` 跟 04-bert 的内容一模一样，连 `MyBertModel` 类的结构都完全一致。蒸馏的 Teacher-Student 架构在哪？温度系数 T 在哪？KL 散度损失在哪？剪枝的 mask 在哪？

**追问**：不要找借口。如果蒸馏和剪枝实际没实现，你现在在这里给我口述一遍知识蒸馏的完整训练流程——从 Teacher 推理到 Student 反向传播，每一步的梯度流向说清楚。

---

## 第3题 · 安全红线

你的 `.env` 文件里直接写死了 DeepSeek API Key，而且我注意到这个文件没有被 `.gitignore` 保护——它已经躺在项目目录里了。

**追问**：假设这个 Key 已经泄露到了公开仓库，从你发现到止损，你的完整操作链路是什么？每一步具体命令说出来。

---

## 第4题 · BERT 动态量化

你在 `bert_quantization.py` 里只量化了 `nn.Linear` 层。BertModel 里面除了 Linear 还有 `LayerNorm`、`Embedding`、Self-Attention 里的 `matmul` 操作。

**追问**：为什么只选 Linear？LayerNorm 为什么不量化？如果我把 Embedding 层也量化到 INT8，会出什么问题？别猜，想清楚再说。

---

## 第5题 · 数据泄漏

你的 `data_eda.py` 里统计了句子长度分布来决定 `max_len`，这个操作本身没问题。但我注意到你三个数据集（train/dev/test）全部加载并打印了。EDA 阶段你用到了测试集吗？如果你用测试集的分布来指导了超参选择，这算不算数据泄漏？

**追问**：别含糊，"没有直接拿测试集调参"不够——你加载了、你看了、你的大脑做了判断。严格意义上，什么操作算"污染测试集"？画一条清晰的边界。

---

## 第6题 · 过拟合诊断

假设你的 BERT 模型训练结果显示：train_acc=0.99，val_acc=0.88，你会怎么判断？是过拟合吗？如果是，你的代码里做了哪些防止过拟合的措施？哪些没做？

**追问**：你用了 `weight_decay=1e-2`，却没配 Early Stopping，也没 LR Scheduler。你的 `epochs=5` 这个数怎么来的？拍脑袋还是看了验证曲线？如果我现在让你把模型训练到"极致"——你会增加什么、改什么？按优先级排序给我。

---

## 第7题 · TF-IDF 方案的致命缺陷

你给了一个例子：`"中国队打败日本队"` 和 `"日本队打败中国队"`，说 TF-IDF 区分不了。OK，这个知道。

**追问**：你的 `rf_predict_fun.py` 里对输入文本做了 `jieba.lcut(text)[:40]`，截断前 40 个词。如果关键信息在后面 40 个词里，怎么办？为什么不截断而是直接全量传给 TF-IDF？TF-IDF 的 `transform` 对超长文本的 OOV 词怎么处理？你的词表大小是多少？没算过？

---

## 第8题 · 大模型方案的成本核算

你 DeepSeek API 调用里同时开了 `reasoning_effort="high"` 和 `thinking: {"type": "enabled"}`。这是一个 10 分类任务，每个样本就是一句话分类。

**追问**：开 reasoning + thinking 比不开多花多少 token？你的每条样本推理大概消耗多少 token？如果 QPS=100，一小时要花多少钱？你算过吗？在这个场景下，开 reasoning_effort="high" 的收益能覆盖成本吗？用数据说服我。

---

## 第9题 · FastText 自动调参

你有 `fasttext_char_2_auto.py` 和 `fasttext_word_2_auto.py` 两个"自动调参"版本。

**追问**：FastText 的 `autotune` 内部做了什么？搜索空间是什么？它和网格搜索有什么区别？如果你的训练集有 10 万条，autotune 需要跑多久？会不会比手动调参还慢？你做字符级和词级两组实验 —— 哪组理论上应该更好？为什么？你的实验结果支持这个预期吗？

---

## 第10题 · 模型部署的生产级问题

你的 FastAPI 服务现在是单进程 `uvicorn.run(app, host=..., port=...)`。假设上线后 QPS 突然从 10 涨到 1000，你的服务会发生什么？

**追问**：你说"加 worker"——那 `uvicorn.run` 里加 `workers=4` 够吗？和 Gunicorn + Uvicorn workers 的区别是什么？模型加载在哪里？每个 worker 都独立加载一份模型到内存吗？一个 BERT 模型 ~400MB，4 个 worker 就是 1.6GB——你确认你服务器的内存够吗？

---

## 第11题 · 文本预处理的一致性问题

你的 RF 方案在训练时和推理时对文本的处理路径不一致——训练时 `data_process.py` 做了什么预处理，推理时 `rf_predict_fun.py` 里又是怎么处理的？

**追问**：如果训练时 jieba 分词去掉了停用词，但推理时忘记了——会发生什么？词汇表不匹配的情况下 TF-IDF 的行为是怎样的？会报错还是静默出错？如果你不确定，现在就去看代码，告诉我答案。

---

## 第12题 · 标签不均衡

你的 README 写了类别不均衡处理是待改进项。你的数据里 10 个类别的分布到底均不均衡？你看过吗？

**追问**：如果不均衡，最少的类别只有 100 条样本，最多的有 5000 条——你会怎么处理？Focal Loss、重采样、类别权重——选哪个？为什么？别三个都列一遍，给我最优解和理由。

---

## 第13题 · Pooler Output vs CLS vs Mean Pooling

你的 `MyBertModel.forward` 只用了 `pooler_output` 做分类。BERT 的 `pooler_output` 是怎么来的？它和 `last_hidden_state[:, 0, :]`（即 CLS token）有什么不同？

**追问**：如果你要做句子级别的语义表示，`pooler_output`、CLS token、所有 token 的 mean pooling、max pooling 哪个通常更好？为什么？你的代码里选了哪个？你做过对比实验吗？没做过的话你凭什么选它？

---

## 第14题 · 代码质量和工程化

你的 `collate_fn=lambda x: collate_fn(x)` 这个 lambda 包装是多余的——`collate_fn=collate_fn` 就行。你的 `config.py` 里 `__init__` 方法超过 50 行，里面混杂了路径初始化、`os.makedirs`、文件 I/O（读 class.txt）。每个方案的 `config.py` 几乎一样，但有细微差别。

**追问**：你这个项目的代码质量，如果要打分（满分 10 分），你给自己打几分？扣分的点你心里清楚吗？如果这是给公司交付的生产代码，你觉得能过 code review 吗？

---

## 第15题 · 综合压力题

你现在花了这么多精力做了 6 条技术路线，从 TF-IDF 到 DeepSeek，看起来很全面。但我问一个尖锐的问题：**你把所有方案都做了一遍，但你真正理解的有几个？**

如果我现在给你一个全新场景——比如做一个中英文混合的垃圾邮件检测，训练集 100 万条——你会选哪条路线？为什么？你在这个项目里学到的最核心的、可以迁移的能力是什么？

**追问**：别说"学到了全链路"。说一个具体的技术判断——在这个项目之前你不懂，做完之后你真正懂了的东西。如果答不出来，那这个项目对你的技术成长价值有限。说吧。
